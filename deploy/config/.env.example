PANDA_ENV=dev
# The identity database; the other per-service databases are created by the
# initdb script in deploy/compose/dev/initdb/.
POSTGRES_DB=panda_identity
POSTGRES_USER=panda
POSTGRES_PASSWORD=replace-me
# 身份库的连接串。**没有任何服务读它**：每个拥有数据库的服务只认自己那一份，读不到就
# 拒绝启动（见 platform/config 的 resolveDatabase），Config 上也不再留那几个共享库字段。
# 还读它的是三个**独立命令**，它们不是服务、不在这条规则里：
#   - user-service/cmd/verify 只读它（连的就是身份库，它查 admin_users）；
#   - user-service/cmd/seed 与 merchant-service/cmd/backfill-region 优先各自的
#     *_DATABASE_URL，只在**单库栈**里回落到它。
# 三种情况为空时都是报错退出，没有静默的路径。
DATABASE_URL=postgres://panda:replace-me@localhost:5432/panda_identity?sslmode=disable
# 下面每一行都是**必填**：对应的服务读不到自己这一份就起不来，没有回退。
USER_DATABASE_URL=postgres://panda:replace-me@localhost:5432/panda_identity?sslmode=disable
MERCHANT_DATABASE_URL=postgres://panda:replace-me@localhost:5432/panda_merchant?sslmode=disable
COUPON_DATABASE_URL=postgres://panda:replace-me@localhost:5432/panda_coupon?sslmode=disable
COFFEE_MACHINE_DATABASE_URL=postgres://panda:replace-me@localhost:5432/panda_coffee_machine?sslmode=disable
ORDER_DATABASE_URL=postgres://panda:replace-me@localhost:5432/panda_order?sslmode=disable
PAYMENT_DATABASE_URL=postgres://panda:replace-me@localhost:5432/panda_payment?sslmode=disable
# 资产账户域（福卡）。它是最后加进来的服务，所以既有的数据卷里没有这个库：initdb 只在
# 首次初始化时跑，老卷要手工 createdb + `panda-migrate ... apply account`。
ACCOUNT_DATABASE_URL=postgres://panda:replace-me@localhost:5432/panda_account?sslmode=disable
# 抽奖域。同 account 的理由：initdb 只在首次初始化时跑，老卷要手工 createdb +
# `panda-migrate ... apply lottery`。
LOTTERY_DATABASE_URL=postgres://panda:replace-me@localhost:5432/panda_lottery?sslmode=disable
# 会员域。同 account 的理由：initdb 只在首次初始化时跑，老卷要手工 createdb +
# `panda-migrate ... apply membership`。
MEMBERSHIP_DATABASE_URL=postgres://panda:replace-me@localhost:5432/panda_membership?sslmode=disable
# 开放平台（合作方接入）域。同 account 的理由：initdb 只在首次初始化时跑，老卷要手工
# createdb + `panda-migrate ... apply partner`。
PARTNER_DATABASE_URL=postgres://panda:replace-me@localhost:5432/panda_partner?sslmode=disable
# Connections each service instance may open. Pinned rather than left to pgx's
# max(4, NumCPU) so the arithmetic is checkable:
#
#     DB_POOL_MAX_CONNS × (services with a database) × (replicas) < max_connections
#
# With the default 10 and eight owning services, one replica wants 80 and the
# stock Postgres max_connections=100 has room for one. Raise max_connections in
# step with replicas; the moment the sum crosses it, every service reports
# "too many clients" at once, not just the one that overshot.
#
# A per-DSN pool_max_conns is honored when this is blank, so either place works.
# An unparseable or non-positive value is an error, not a silent fallback —
# starting with a connection count nobody chose only shows up under load.
DB_POOL_MAX_CONNS=10
# Applies the service's migration set during startup. Production runs migrations
# out of band, so this stays false there.
DB_MIGRATE_ON_START=false
# etcd endpoints for service discovery. Empty means the no-op registry: services
# run without registering, which is how local stacks start. A value that is set
# but unreachable is NOT treated as empty — the service refuses to start rather
# than serve unregistered.
# ETCD_ENDPOINTS is still accepted as a deprecated alias.
REGISTRY_ENDPOINT=http://etcd:2379
RABBITMQ_URL=amqp://user:replace-me@rabbitmq:5672/
RABBITMQ_USER=user
RABBITMQ_PASSWORD=replace-me
# The topic exchange every event is published to, and the pattern this service's
# queue binds. A blank routing key binds "#" (everything) — an empty binding
# would strand every event on a topic exchange. A comma-separated list binds the
# queue once per pattern (account-service needs four: one event type grants
# fortune cards, three more freeze and release them).
RABBITMQ_EXCHANGE=panda.events
RABBITMQ_ROUTING_KEY=
# Only services that consume need this. Publishers (merchant-service's outbox
# relay) work without it and never declare a queue.
#
# ⚠️ 这两行是**按服务**取的，而它们之间没有任何隔离：手工起一个消费者时若沿用别处那份
# .env，它会认领**那个**队列并开始 ack 里面的消息。实测过的一幕是 membership-service 挂上
# 身份审计的 admin.operation.logged 队列——审计中继那侧再也收不到，而两边都不报错。
# compose 里每个消费者各写各的（order-service 是 order.payment.dev / "payment.*"——它那个
# 通配**只对两段的键成立**）；
# 不在 compose 里的服务要手工给：membership-service 消费 order.paid 与支付域的四条事件（两条协议
# 变更 + 两条某一期代扣的结果），用 membership.order.paid.dev /
# order.paid,payment.agreement.signed,payment.agreement.terminated,payment.agreement.charge_succeeded,payment.agreement.charge_failed
# （后四条是**三段**的键，payment.* 匹配不到它们，见 payment-service 的 dto）；coupon-service 消费会员域的三条发券事件，
# 用 coupon.membership.dev / membership.activated,membership.renewed,membership.campaign.claimed
# （逗号分隔可绑多个路由键，见 platform/messaging/rabbitmq.go）。
RABBITMQ_QUEUE=
# Failed deliveries are retried this many times before they are rejected into the
# dead-letter queue. 0 disables retries — but rejections then still dead-letter
# rather than vanish, because the queue keeps its x-dead-letter-exchange.
RABBITMQ_RETRY_LIMIT=5
# Where rejected messages end up. Both default to panda.events.dlx / .dlq; set
# them blank to keep the defaults. They exist so a poison message is inspectable
# and replayable instead of silently dropped.
RABBITMQ_DLX=
RABBITMQ_DLQ=
# NOTE when adding the dead letter topology to a queue that already exists: the
# x-dead-letter-exchange argument is part of the queue's identity, and RabbitMQ
# rejects a redeclare that changes it (406 PRECONDITION_FAILED). On an existing
# broker, either delete the queue first or apply the arguments with a policy.
OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m
TEMPO_ENDPOINT=tempo:4317
OPENSEARCH_ENDPOINT=http://opensearch:9200
PROMETHEUS_ENDPOINT=0.0.0.0:8889
# What the services actually read. A blank value means the no-op providers: no
# exporter, no network activity, and the tracing/metrics middleware instruments
# nothing. Point it at otel-collector's OTLP gRPC receiver to turn both on.
OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=replace-me

# Redis carries exactly two things: the policy-change broadcast that keeps every
# user-service replica's Casbin snapshot current, and the rate-limit window the
# gateway counts against. It never caches authorization decisions — those are
# re-read live on every request. An empty REDIS_ADDR is supported: the broadcast
# degrades to "this instance only" and rate limiting falls back to an in-process
# token bucket (so the budget becomes per-replica instead of global).
#
# Only user-service (broadcast) and gateway-service (rate limit) dial this.
# merchant-service does not, and an unreachable Redis no longer stops it from
# starting — so a Redis maintenance window costs admin permission propagation
# and per-replica rate accuracy, not the merchant domain.
REDIS_ADDR=
REDIS_PASSWORD=
REDIS_DB=0
# Gateway edge rate limiting, per client IP. Auth endpoints get their own,
# tighter budget because that is where credential stuffing lands.
# RATE_LIMIT_ENABLED=false turns the whole thing off.
RATE_LIMIT_ENABLED=true
RATE_LIMIT_PER_MINUTE=600
RATE_LIMIT_AUTH_PER_MINUTE=30
# Which peers may speak for the client in X-Forwarded-For, as a comma-separated
# list of CIDRs. Empty means "trust nobody" and keys every request on
# RemoteAddr — correct when clients reach the gateway directly.
#
# Set this as soon as a load balancer sits in front of the gateway. Without it
# every request arrives with the LB's address as RemoteAddr, so the whole site
# shares one 600/min bucket and everyone gets 429 together.
# TRUSTED_PROXY_CIDRS=10.0.0.0/8,192.168.0.0/16
#
# Only hops from these ranges are walked, right to left, and the first
# untrusted address wins — so a client that forges X-Forwarded-For still cannot
# pick its own bucket. Never use 0.0.0.0/0 here: that trusts every peer.
TRUSTED_PROXY_CIDRS=
# The HS256 signing key shared by every service (auth.NewService). Must be
# replaced before this file is used as a live .env: the value below is published
# in the repository, so a stack left on it accepts tokens anyone can mint.
# auth.NewService refuses to start on that value — it is on a fixed blocklist,
# compared exactly against the trimmed setting, so whitespace around it does not
# help — and on anything shorter than 32 bytes. Generate one with, e.g.,
# `openssl rand -hex 32`.
JWT_SECRET=replace-with-a-long-random-secret
JWT_ISSUER=panda-v2
USER_SERVICE_URL=http://user-service:8080
MERCHANT_SERVICE_URL=http://merchant-service:8080
# Gateway only. 券域只有后台一棵树（/v1/admin/coupons，见 gateway 的 proxy.go），所以少了
# 这一行的症状只有一种：网关自己的 404，页面上写「接口不存在」。Blank is supported.
COUPON_SERVICE_URL=http://coupon-service:8080
# Static internal gRPC endpoints, used until service discovery is enabled.
# user-service calls merchant-service and vice versa, so each needs its PEER's
# address; a service's own gRPC listener is GRPC_ADDR.
USER_GRPC_ADDR=user-service:9090
# coffee-machine-service 读它：设备挂点位前先问这个点位在不在（client.StoreResolver）。
# lottery-service 也读它，而且两条路都要：开通抽奖前问一次「这家店存在吗」（问不到就
# 不受理，否则会留下一条永远查无此店的开通记录），读列表时再解一页门店名——抽奖库只存
# 门店 id，名字不落库（migrations/lottery）。两边都拒绝启动于空地址。
MERCHANT_GRPC_ADDR=merchant-service:9090
# order-service 也读它：下单时要问 coffee-machine-service 这台设备在不在、启没启用、
# 挂的点位对不对（方案 5.8）。缺了它 order-service 起不来——地址缺席不是「少个可选
# 依赖」，那条校验会整条失效（见 platform/config 里的拒绝启动）。
COFFEE_MACHINE_GRPC_ADDR=coffee-machine-service:9090
# order-service 也读它：发起支付那条链路（方案 7.2）由它编排——金额与归属在订单侧
# 校验完，再同步调 payment-service 建支付单。payment-service 自己不读这个变量，它
# 的监听地址是 GRPC_ADDR。缺了它 order-service 起不来：不是「少个可选依赖」，而是
# 订单建得出来却付不了款，只能停在 pending_payment。
PAYMENT_GRPC_ADDR=payment-service:9090
# account-service 自己**不读**它——它的福卡 gRPC 面是被调的一方，监听地址是 GRPC_ADDR。
# 读它的是两个调用方：payment-service（账户出资时扣豆）和 lottery-service（参与抽奖时
# 扣福卡），两个都拒绝启动于空地址。值就是 account-service 的监听地址。
ACCOUNT_GRPC_ADDR=account-service:9090
# membership-service 自己**不读**它——它的 gRPC 面是被调的一方，监听地址是 GRPC_ADDR。
# 读它的是 order-service：带会员行的那一单，价格、时长与会员价路径只能从会员域取
# （下单选套餐就是它在卖的东西）。缺了它 order-service 起不来——不是「少个可选依赖」，
# 而是客户端说自己买的是什么就是什么（见 platform/config 里的拒绝启动）。
MEMBERSHIP_GRPC_ADDR=membership-service:9090
# order-service 自己**不读**它——它这次是**被调的一方**（设备刷卡回调建单，方案 §四），
# 监听地址是 GRPC_ADDR。读它的有两个：
#   - partner-service：那条回调在它那里验完签，再经这条 RPC 把订单记过去。缺了它
#     partner-service 起不来——那条路是「先收钱、后建单」，钱已经在机器上收过了，
#     建不出单就是一笔收了钱却没有单的交易。
#   - membership-service：一期代扣**成功之后**先把这一期记成一张订单（订单管理里看得见、
#     下游 coupon-service 拿到订单号才发会员价券），再回本库续这一期。缺了它同样起不来，
#     而那笔钱已经扣了。
ORDER_GRPC_ADDR=order-service:9090
# Shared service token for internal gRPC calls, in both directions.
MERCHANT_INTERNAL_TOKEN=replace-with-at-least-32-byte-service-token
# Ownership timeout: 1000..30000 milliseconds.
MERCHANT_OWNERSHIP_TIMEOUT_MS=5000
GATEWAY_ADDR=:8080
GATEWAY_REQUEST_TIMEOUT_MS=10000
# The upload route gets its own, larger budget instead of the 10s every other
# route is held to: it covers the client's upload AND the write to OSS. 10MiB at
# 1Mbps upstream is ~80s, so 60s would produce a steady stream of 504s. Keep it
# above merchant-service's HTTP_TIMEOUT_MS — this is the outer of the two.
GATEWAY_UPLOAD_TIMEOUT_MS=120000
COFFEE_MACHINE_SERVICE_URL=http://coffee-machine-service:8080
# Gateway only. Blank is supported: /v1/admin/orders and /v1/miniapp/orders then
# stay 404 instead of being forwarded. The two paths are listed separately in the
# gateway because /v1/miniapp/orders would otherwise fall through to user-service
# under the /v1/miniapp prefix.
ORDER_SERVICE_URL=http://order-service:8080
# Gateway only, and the one upstream that is not for our own front ends: 渠道回调从
# 公网打进来（渠道签得了名，但进不了内网），所以这个值必须是**渠道能到达的地址**
# ——生产是公网域名，本地是网关自己的地址。Blank is supported: /v1/payments then
# stays 404 instead of being forwarded. 发起支付**不**走这条路（那是 order-service
# 到 payment-service 的 gRPC），网关不为它转一行。
PAYMENT_SERVICE_URL=http://payment-service:8080
# Gateway only. Blank is supported: the two fortune-card paths then stay 404 instead of
# being forwarded. The two are listed separately for the same reason as the order domain:
# /v1/miniapp/fortune-cards would otherwise fall through to user-service under the
# /v1/miniapp prefix, and the client's 404 would read as "user-service has no such API".
ACCOUNT_SERVICE_URL=http://account-service:8080
# Gateway only. Blank is supported: the lottery paths then stay 404 instead of being
# forwarded. Listed separately for the same reason as the order and account domains:
# /v1/miniapp/lottery/* would otherwise fall through to user-service under the
# /v1/miniapp prefix, and the client's 404 would read as "user-service has no such API".
# There is deliberately no LOTTERY_GRPC_ADDR here — the service has no inbound gRPC.
LOTTERY_SERVICE_URL=http://lottery-service:8080
# Gateway only. Blank is supported: the membership paths then stay 404. The one upstream with
# a tree on each end. Listed separately for the same reason as the order and account domains:
# /v1/miniapp/membership and its subpaths (/plans, /auto-renew, /campaigns/claim) would
# otherwise fall through to user-service under the /v1/miniapp prefix, and the client's 404
# would read as "user-service has no such API". The admin half has no such conflict and would
# simply be the gateway's own 404 — it is four prefixes, because prefix matching is
# segment-wise: /v1/admin/memberships does not cover /v1/admin/membership-subscriptions.
# The four are membership-plans, membership-subscriptions, membership-campaigns, memberships.
#
# These prefixes must match membership-service's own route table word for word (its
# internal/routes). One missing prefix is a 404 neither side reports: the routes over there
# are fine, so nothing looks wrong.
#
# Note the two admin prefixes are separate lines, not one /v1/admin/membership prefix: the
# match is by path segment, so /v1/admin/membership would match neither of them.
MEMBERSHIP_SERVICE_URL=http://membership-service:8080
# Gateway only, and like PAYMENT_SERVICE_URL it is an upstream for people who are **not our
# front ends**: 合作方从公网打进来，签名是他们自己算的。所以这个值必须是**合作方能到达的
# 地址**——生产是公网域名，本地是网关自己的地址，而不是 http://partner-service:8080
# （合作方解析不了容器名，也到不了那个端口）。
# Blank is supported: the three partner prefixes then stay 404 instead of being forwarded.
#
# ⚠️ 这三条前缀在网关的 switch 里是**分开的三条**，不是一条 /v1/admin/partner：匹配按
# 路径段走，所以 /v1/admin/partner 两条都不匹配。
#
# /v1/openapi 那一条**刻意留粗**：它下面今天有三条接口（会员权益查询、设备刷卡回执、
# 设备取货码），三条都归合作方域、都要过同一套验签，所以**不为设备子树单开 case**——
# 设备密钥就存在合作方服务的凭据表里，拆成两个上游等于让那两条设备接口绕开验签。
# 理由写在网关 proxy.NewHandler 里那一段（"别再给 /v1/openapi 下面加更细的 case"）。
#
# 这些前缀必须与 partner-service 自己的路由表逐字一致（/v1/admin/partners、
# /v1/admin/partner-call-logs、/v1/openapi/member-price-entitlement）。少一条的表现是
# 一个两边都不报的 404：那边的路由是好的，所以看起来什么都不像错。
PARTNER_SERVICE_URL=http://partner-service:8080
# payment-service only，而且它是**自己的**公网地址而不是谁的：建支付单时把这个基址
# 交给渠道，渠道照着它回调 /v1/payments/callback/{channelCode}。生产填公网域名，
# 不是内网服务名。缺了它拒绝启动——支付单建得出来、钱也可能收得到，但结果永远回不到
# 我们这边（钱收了、单停在 pending）。
PAYMENT_NOTIFY_BASE_URL=http://gateway-service:8080
# payment-service only，**可选**：H5 支付用户付完从收银台跳回我们这边之后，再把他送一程
# 的那个页面地址（模板里的 {merOrderId} 会被换成验签之后拿到的订单号；没有占位符就原样跳）。
# 空 = 不跳转，回 200 + 一句纯文本——这条路上**一行都不写库**，钱到没到早已由回调或查单
# 定过，所以配不配都不影响支付状态。
# 它放在部署环境而不是渠道 config：渠道 config 里是「怎么跟渠道说话」，结果页在哪儿是
# 我们的部署事实。真与渠道有关的回跳地址由 PAYMENT_NOTIFY_BASE_URL 拼出来。
PAYMENT_RETURN_PAGE_URL=
# ---------------------------------------------------------------------------
# 银联商务全民付（payment-service only）
#
# 这九行以前是后台「支付渠道」页上的九个输入框，存进 payment_channels.config 那一列，
# 由运营一栏一栏地填。现在它们是**这家公司在渠道侧登记的标识**——跟着部署走，跟着代码走
# （见 payment-service 的 internal/catalog）。
#
# 上面六个是**账户值，不是密钥**。留空是合法的：一个只跑咖啡豆支付的部署根本用不到这一族，
# 让它起不来代价大于收益。拦截点在使用那一刻——用户选中银联商务时，报的错会点名缺的是
# 下面哪一个变量。
PAYMENT_UMS_BASE_URL=
PAYMENT_UMS_APP_ID=
PAYMENT_UMS_MID=
PAYMENT_UMS_TID=
PAYMENT_UMS_SOURCE_CODE=
PAYMENT_UMS_DOMAIN_NAME=
# 下面三个是**这个应用自己的**应用标识，只影响 H5 的微信那两条路（渠道文档标为必填，
# 苹果传 bundle id、安卓传包名、手机网站传首页 URL）。留空时那几个字段**不带**而不是带空串
# ——带了空的应用名渠道可能受理下来，那会让一条没配好的渠道看上去能跑。
PAYMENT_UMS_SCENE_TYPE=
PAYMENT_UMS_MER_APP_NAME=
PAYMENT_UMS_MER_APP_ID=
# 银联商务的两把对称密钥，方向相反：
#
#   PAYMENT_UMS_APP_KEY   —— 出站：我们算签名给渠道验。下单、查单、退款都走它。
#   PAYMENT_UMS_COMM_KEY  —— 入站：渠道算签名给我们验。H5 的支付结果通知与结果页回跳走它。
#
# 它们**不出现在上面那组账户值里**，也不进 platform/config 的结构体：那个结构体会被打印、
# 会被 %+v 到日志里，密钥进去之后离泄漏只差一次调试。装配处只持有**变量名**
# （见 catalog.Channel.SecretEnv），签名那一刻才 os.Getenv。
#
# 这两行只放占位值，真密钥进本地那份不入库的 .env。**两把必须是不同的值**：都填同一个的话，
# 入站验签会拿 appKey 去验通讯密钥的签——它不会被拦下（密钥非空），只会一路验不过，而日志里
# 只有一句「签名不对」。本地联调时两把用同一个值可以绕开（假服务端也按那个值签）。
#
# 读不到密钥时适配器**拒绝**（ErrSecretNotConfigured），不是跳过验签——H5 的回调会一律被拒、
# 支付单停在 pending，而钱在渠道那边。
PAYMENT_UMS_APP_KEY=replace-me
PAYMENT_UMS_COMM_KEY=replace-me
# ---------------------------------------------------------------------------
# 微信直连委托代扣（payment-service only）
#
# 连续包月会员的**签约**通道：用户在微信里授权之后，商户按月扣款。它与上面那组 PAYMENT_UMS_*
# 是**两套完全不同的东西**——不同的商户号、不同的密钥、不同的签名口径（APIv2 是 XML + MD5，
# 银联商务是 JSON + 签名头），在 payment-service 里是两个适配器。V2 的付款仍然走银联商务，
# 这条通道只管「以后每个月自动扣」这份授权（见 internal/provider/wechatpay 的包注释）。
#
# 这组值就是**老系统 panda_serve 那套**（configs/config.yaml 的 `wechat.pay` 段）：用户已
# 拍板签约复用老系统的商户号与实现方式。生产值不进仓库，这里只放占位。
#
# 留空是合法的：一个不卖连续包月的部署用不到这一族，与上面那组同一条理由（缺项会在选中这条
# 通道时被点名，错误串里写着缺的是哪个变量名）。
WECHAT_PAY_BASE_URL=
WECHAT_PAY_APP_ID=
WECHAT_PAY_MCH_ID=
# 跳转签约那一步的目标：**微信官方签约小程序**的 appid。
#
# 纯签约（小程序）不发任何出网请求，服务端只算出一张参数表交给客户端，客户端拿它调
# `wx.navigateToMiniProgram`，用户在**那个**小程序里点「同意并签约」。所以这张表里有两个
# appid：待签串里是我们自己的（WECHAT_PAY_APP_ID），跳转目标是微信官方的，就是这一行。
#
# 它以前写死在 provider/wechatpay/config.go 里，理由是「微信侧的固定值，换一个值等于跳去
# 另一个不存在的小程序」。那说的是**取值只有一个**，推不出**它可以公开**：它是一串能定位到
# 具体主体的标识，与商户号同一性质。协议固定 ≠ 可以提交进仓库（GitHub 密钥扫描在 commit
# 4920ce71 上标了它）。
#
# 留空是合法的：一个不做签约的部署用不到它，缺了会在选中这条通道时被点名（错误串里写着
# 缺的是哪个变量名）。但**只跑纯签约的部署必须有它**：缺了算出来的参数表导不了跳，而失败
# 发生在用户手机上（点了没反应），不在日志里。
WECHAT_PAY_SIGN_MINI_PROGRAM_APP_ID=
# 商户证书与私钥的**文件路径**（apiclient_cert.pem / apiclient_key.pem，微信商户平台下发）。
#
# 它们是路径而不是密钥：装进容器的是文件本身（compose 里挂一个只读卷）。**只有两条路要
# 证书**——代扣（/pay/pappayapply）与解约（/papay/deletecontract）；纯签约一次出网请求都没有、
# 查约不要证书。所以两个都留空时，签约与查约照样能用，代扣与解约会明确拒绝（不是降级成不带
# 证书发出去——那样扣款到底发生没发生，我们再也说不清）。
#
# 只填一个是**等价于没填**：不去猜「另一个是不是有默认路径」。
WECHAT_PAY_CERT_PATH=
WECHAT_PAY_KEY_PATH=
# APIv2 密钥（32 位），**签名与验签共用同一把**——微信 APIv2 的回调就是用同一把密钥按同一套
# 算法签的，所以这一族只有一个密钥槽。
#
# 它不出现在上面那组里，也不进 platform/config 的结构体：那个结构体会被打印、会被 %+v 到
# 日志里，密钥进去之后离泄漏只差一次调试。装配处只持有**变量名**（见 catalog.Channel.SecretEnv），
# 签名那一刻才 os.Getenv——与上面两把银联商务密钥走的是同一条路。
#
# 读不到时签约请求**拒绝**（ErrSecretNotConfigured），不是算一个空签名发出去：空签名在微信
# 那边是一句 SIGN_ERROR，看上去像我们签名算法写错了。
WECHAT_PAY_API_V2_KEY=replace-me
# 这里以前还有一行 PAYMENT_SECRET_KEY（封渠道凭据的主密钥）。**删了**：渠道表连同它的密文
# 列都不建（migrations/payment），收钱那四种写在 payment-service 的 internal/catalog
# 里，密钥走上面那两行、按变量名现读，没有要解的密文，也就不需要主密钥。payment-service 今天
# 一行 platform/secret 的代码都没调用（config 里那条「读不到就拒绝启动」的守卫跟着撤了，
# 有测试钉着：TestLoadPaymentServiceDoesNotNeedACredentialMasterKey）。
#
# 加密合作方**签名密钥**的主密钥（AES-256-GCM，见 platform/secret）。与上面那条同一套规则、
# 同一批理由：**只有 partner-service 读它，读不到就拒绝启动**，占位值本身合格（解出来正是
# 32 字节），生产换 `openssl rand -base64 32`，**换掉它库里已有的密文就解不开了**——
# 那意味着所有合作方的密钥都要重签（这件事比渠道凭据重配更贵：密钥在对方手里）。
#
# 全仓今天只有这一把主密钥：platform/secret 的唯一调用方就是 partner-service（支付那边
# 随渠道表一起撤了）。partner-service 的 config 校验对它是**单独一条**（见 platform/config
# 里 partner-service 那段）。真再接上一个要用密文的东西时，新开一把而不是共用这一把——
# 一次轮换同时冻住两件不相干的事，是这个仓库踩过的形状。
PARTNER_SECRET_KEY=ZGV2LW9ubHktcGFydG5lci1tYXN0ZXIta2V5LTMyYiE=
# partner-service only，逗号分隔的 CIDR 列表（也接受裸地址，裸 IPv4 按 /32、裸 IPv6 按
# /128）。它回答的是「谁写的 X-Forwarded-For 可以信」——合作方永远从网关进来，不看这一跳
# 的话所有请求的来源都是网关自己，于是 IP 白名单要么全拒要么全放，而两者都不报错。
#
# **留空是合法且安全的默认**：留空 = 什么都不信 = 只看 RemoteAddr。本地直连（curl 打
# partner-service 的 8080）正是这个场景。容器里走网关时必须配上网关所在的网段，否则配了
# IP 白名单的合作方从公网调过来会全部 401 + IP_NOT_ALLOWED——失败关闭，但看上去像对方填错
# 了 IP。配错（写域名、写错格式）会让服务**拒绝启动**，不会静默退回。
PARTNER_TRUSTED_PROXY_CIDRS=
# Each service's own listeners. In containers these are the ports the service
# name resolves to; the gRPC ones are also what gets registered for discovery.
HTTP_ADDR=:8080
GRPC_ADDR=:9090
# Whole-request timeout: kratos installs it around the request, body included, so
# it also bounds a single upload rather than one step of it. Empty means the code
# default — 5s for every service, 90s for merchant-service, because a 10MiB upload
# cannot finish in 5s. The three budgets layer, outer to inner: gateway upload
# 120s > merchant-service 90s > the OSS SDK's 60s read/write.
HTTP_TIMEOUT_MS=
SERVICE_VERSION=0.1.0

# Image uploads are written to Alibaba Cloud OSS. The key names are the ones the
# legacy panda_serve stack already uses, so moving over is a straight copy of
# four lines — but only the names, never the values: put the real pair in your
# local, untracked .env. A completely missing block is a supported state —
# merchant-service starts normally and only the upload endpoint answers 503,
# naming exactly which of these is missing.
OSS_ACCESS_KEY=replace-me
OSS_SECRET_KEY=replace-me
# Bare endpoint, WITHOUT the bucket. Public object URLs are built from it as
# https://<bucket>.<endpoint>/<key> unless OSS_CNAME below is set.
OSS_ENDPOINT=oss-cn-hangzhou.aliyuncs.com
OSS_BUCKET=replace-me
# Custom domain bound to the bucket. Set it and object URLs go through that domain
# instead; leave it empty to derive the base from OSS_ENDPOINT. Deliberately the
# only name for this setting — the legacy CDN_URL meant the same thing.
OSS_CNAME=
# Object key prefix. This is what keeps v2's objects apart from the legacy site's
# inside one bucket. Point it at the legacy prefix only if you mean to reuse
# those objects.
UPLOAD_PATH=panda-v2
# Per-file limit: 10MiB. The ceiling enforced in code is 64MiB, because a larger
# body cannot finish inside GATEWAY_UPLOAD_TIMEOUT_MS — see the note above.
UPLOAD_MAX_FILE_SIZE=10485760
# Kept only so a copied legacy config still loads. The uploader always uses
# content-addressed md5 keys — a key built from the client filename can escape
# UPLOAD_PATH — so false is ignored, with a warning at startup.
UPLOAD_USE_MD5=true

# Development account initialization is disabled by default and only works with PANDA_ENV=dev.
# Set passwords through your local environment; never commit real values.
DEV_ACCOUNT_INIT_ENABLED=false
DEV_ADMIN_USERNAME=admin
DEV_ADMIN_PASSWORD=
DEV_MERCHANT_USERNAME=merchant
DEV_MERCHANT_PASSWORD=

# WeChat miniapp login (user-service). These names say what they are, so moving
# them over from the legacy backend is a straight copy — but only the names,
# never the values. A completely missing block is a supported state: the two
# WeChat login paths answer 503 and name which variable is missing, while SMS
# login and every other miniapp endpoint keep working. Put the real pair in your
# local, untracked .env.
WECHAT_MINIAPP_APP_ID=replace-me
WECHAT_MINIAPP_APP_SECRET=replace-me
# Only for a proxy or a sandbox. Empty means the real api.weixin.qq.com; the
# WeChat access token is fetched through /cgi-bin/stable_token, which returns the
# same token to every caller, so several replicas can each cache it safely.
WECHAT_MINIAPP_API_BASE=

# Logs SMS verification codes instead of sending them, so a stack without a
# provider can still exercise SMS login. Refused unless PANDA_ENV=dev, and off by
# default: a code in a log file is a credential that logs into an arbitrary phone
# number. Leave it false anywhere real — without it the endpoint answers 503
# rather than pretending the message went out.
SMS_DEV_LOG_CODES=false
