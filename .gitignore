# secrets and local state
.env
.env.*
!.env.example
*.local
node_modules/
dist/
coverage/
*.tsbuildinfo
.omc/
.DS_Store

# generated frontend artifacts
.umi/
.umi-production/
**/.umi/
**/.umi-production/
.claude/worktrees/

# compiled local binaries
# `go build ./backend/tools/fakechannel` 默认把产物落在包目录的上两级，也就是这里。
/backend/fakechannel

# 编译产物：`go build` 或 make 会把服务二进制落在各自的 bin/ 下。
# membership-service/bin/membership-service（37 MB）曾经被跟踪进去过，已从索引删除。
**/bin/
